global:
  scrape_interval: 10s # частота сбора

scrape_configs:
  - job_name: 'airflow' # по этому имени потом будем "искать" нужный индекс
    static_configs:
      - targets: ['statsd-exporter:9102'] # Prometheus-метрики Airflow через StatsD exporter
